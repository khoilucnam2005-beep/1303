<Window x:Class="_1303.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_1303"
        mc:Ignorable="d"
        Title="MainWindow" Height="957" Width="733">
    <Grid Margin="10,10,10,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="6*"/>
                <ColumnDefinition Width="4*"/>
            </Grid.ColumnDefinitions>

            <GroupBox Grid.Column="0" Header="Nhập số liệu" FontSize="14" FontWeight="Bold" Margin="0,0,10,0">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Label Grid.Row="0" Grid.Column="0" Content="Mã sản phẩm:" VerticalAlignment="Center" FontWeight="Normal"/>
                    <TextBox x:Name="txtMaSP" Grid.Row="0" Grid.Column="1" Height="25" Margin="0,2"/>

                    <Label Grid.Row="1" Grid.Column="0" Content="Tên sản phẩm:" VerticalAlignment="Center" FontWeight="Normal"/>
                    <TextBox x:Name="txtTenSP" Grid.Row="1" Grid.Column="1" Height="25" Margin="0,2"/>

                    <Label Grid.Row="2" Grid.Column="0" Content="Nước sản xuất:" VerticalAlignment="Center" FontWeight="Normal"/>
                    <TextBox x:Name="txtNuocSX" Grid.Row="2" Grid.Column="1" Height="25" Margin="0,2"/>

                    <Label Grid.Row="3" Grid.Column="0" Content="Năm sản xuất:" VerticalAlignment="Center" FontWeight="Normal"/>
                    <TextBox x:Name="txtNamSX" Grid.Row="3" Grid.Column="1" Height="25" Margin="0,2"/>
                </Grid>
            </GroupBox>

            <Grid Grid.Column="1" Margin="10,10,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="1*"/>
                </Grid.RowDefinitions>
                <Button x:Name="btnNhap" Grid.Row="0" Content="Nhập" FontWeight="Bold" FontSize="14" Click="btnNhap_Click" Margin="0,5,0,5"/>
                <Button x:Name="btnChon" Grid.Row="1" Content="Chọn sản phẩm" FontWeight="Bold" FontSize="14" Click="btnChon_Click" Margin="0,5,0,5"/>
                <Button x:Name="btnThoat" Grid.Row="2" Content="Thoát" FontWeight="Bold" FontSize="14" Click="btnThoat_Click" Margin="0,5,0,5"/>
            </Grid>
        </Grid>

        <Button x:Name="btnHienThi" Grid.Row="1" Content="Hiển thị các sản phẩm" Height="35" FontSize="14" FontWeight="Bold" Click="btnHienThi_Click" Margin="0,5,0,5" Background="#FFEE8AC7"/>

        <DataGrid x:Name="dgSanPham" Grid.Row="2" Margin="0,0,0,10" AutoGenerateColumns="False" CanUserAddRows="False" HeadersVisibility="Column" SelectionChanged="dgSanPham_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="No" Width="0.8*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" Margin="5,0,5,0"/>
                                <TextBlock Text="{Binding No}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Mã sản phẩm" Binding="{Binding MaSP}" Width="1.5*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Tên sản phẩm" Binding="{Binding TenSP}" Width="2.5*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Nước sản xuất" Binding="{Binding NuocSX}" Width="1.5*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Năm sản xuất" Binding="{Binding NamSX}" Width="1.5*" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button x:Name="btnLoc" Grid.Row="3" Content="Các sản phẩm sản xuất trước năm 2000" Height="35" FontSize="14" FontWeight="Bold" Click="btnLoc_Click" Margin="0,5,0,5" Background="#FFEE8AC7"/>

        <DataGrid x:Name="dgSanPhamLoc" Grid.Row="4" AutoGenerateColumns="False" CanUserAddRows="False" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="No" Width="0.8*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" Margin="5,0,5,0"/>
                                <TextBlock Text="{Binding No}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Mã sản phẩm" Binding="{Binding MaSP}" Width="1.5*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Tên sản phẩm" Binding="{Binding TenSP}" Width="2.5*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Nước sản xuất" Binding="{Binding NuocSX}" Width="1.5*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Năm sản xuất" Binding="{Binding NamSX}" Width="1.5*" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>